`For now we hardcode an interest rate and assume we will be able to maintain at least
this level of yield through solana defi.  this should probably be set in the database.`;

const USDC_YIELD = 1.15;

// calculate daily yield, and the excess USDC which we keep.

// sweep remaining USDC into our accounts, which we'll store in circuit vaults (?)
